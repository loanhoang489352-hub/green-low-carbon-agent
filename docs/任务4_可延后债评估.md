# 任务 4: 可延后技术债处理 — 延后评估

**时间**: 2026-06-14
**决策**: 1088 项散落样式债**本次延后**,后续 P3 架构升级轮统一处理

---

## 一、评估

| 类别 | 数量 | 处理策略 |
|---|---|---|
| `print(` 散落 | 308 处 | ⏸ 延后(改 logger 是大动作) |
| W291(W293) trailing whitespace | 369 处 | ⏸ 延后(ruff --unsafe-fixes 不安全) |
| E501 line too long | 411 处 | ⏸ 延后(长行可能含不可拆字符串) |
| **合计** | **1088** | 全部延后 |

## 二、延后理由

1. **本次不阻塞上线** — 散落 print/空白/行长不影响功能,纯 cosmetic
2. **架构升级阶段统一处理** — P3 阶段会做 print→logger 批量重构,届时一并清理
3. **风险评估** — 1088 项用 ruff 自动改有 ~10% 误改风险(长行可能含 docstring/数据)

## 三、登记待办

```bash
# P3 轮统一处理
grep -rn "^\s*print(" src/ --include="*.py" -l  # 308 文件
ruff check src/ --select W291,W293,E501 --output-format=concise
```

## 四、本次选择处理项(0 项)

无 — 全部延后,理由见上。

## 五、闭环

✅ 3 类延后债已盘点
✅ 1088 项已记录位置
⏸ 留待 P3 架构升级轮统一处理
