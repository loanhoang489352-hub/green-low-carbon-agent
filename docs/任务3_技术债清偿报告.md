# 任务 3: 4 项硬性必还技术债清偿报告

**清偿时间**: 2026-06-14
**目标**: 必还技术债 4 项 100% 闭环

---

## 一、4 项技术债清偿状态

| ID | 债务 | 状态 | 证据 |
|---|---|---|---|
| P3-1 | 中文文件名兼容处理 | ✅ 已还 | 任务 2: `src/pet/数值规则.py` → `numeric_rules_v1_legacy.py` |
| P3-2 | 鉴权切换收尾 | ✅ 已还 | 任务 2: 新增 2 个 secure 端点(whoami/change_password) |
| P3-3 | RAGAS 适配收尾 | ✅ 已还 | 任务 1: 3 标准指标 + 1 proxy,固化在 `scripts/eval_ragas.py` |
| P3-4 | 宠物单元测试 | ✅ **27/27 全过** | 本任务新增 `tests/test_pet_engine.py` |

---

## 二、宠物单元测试覆盖(任务 3 主产出)

### 2.1 测试结构

`tests/test_pet_engine.py`:6 个 TestClass, 27 个 test method

| TestClass | 测试数 | 覆盖维度 |
|---|---|---|
| `TestBehaviorRewards` | 4 | 行为→奖励(公交/节电/回收/植树)数值同步 |
| `TestAppearanceUnlock` | 3 | 形态解锁(sprout/leaf/guardian/seed) |
| `TestInteractions` | 5 | 抚摸/投喂/币不足/无效道具 |
| `TestPersistence` | 4 | 7 张表持久化 + 跨进程 |
| `TestEdgeCases` | 7 | 异常/边界(空 user/未知行为/负数/封顶/Lv50) |
| `TestSkills` | 3 | 技能解锁/记录 |
| `TestIntegration` | 1 | 端到端用户旅程 |

### 2.2 测试 fixture(隔离)

```python
@pytest.fixture(autouse=True)
def isolated_pet_db(tmp_path, monkeypatch):
    """每个测试用临时 pet.db,避免污染"""
    test_db = tmp_path / "test_pet.db"
    monkeypatch.setattr(constants, "PET_DB_PATH", test_db)
    pe_mod._pet_engine = None  # 重置单例
    constants.init_pet_schema()
    yield test_db
    if test_db.exists():
        test_db.unlink()
```

### 2.3 关键测试样例

```python
# 数值同步
def test_bus_rewards(self):
    r = e.apply_behavior_rewards("u1", "bus", 10.0)
    assert r.new_state.vitality == 58  # 50+8
    assert r.new_state.coins == 2

# 形态解锁
def test_sprout_unlock_at_15kg(self):
    for i in range(3):  # 植树 3 棵 = 15kg
        r = e.apply_behavior_rewards("u1", "plant", 1.0)
    assert r.new_state.appearance == "sprout"

# 边界
def test_state_caps_at_100(self):
    for _ in range(20):
        e.apply_behavior_rewards("u1", "plant", 1.0)
    state = e.get_state("u1")
    assert state.hunger <= 100  # 封顶

# 端到端
def test_full_user_journey(self):
    for _ in range(5): e.apply_behavior_rewards(user, "plant", 1.0)
    e.pat(user); e.feed(user, "solar")
    skills = e.get_skills(user)
    # ...
```

### 2.4 跑测命令

```bash
cd D:\绿色低碳智能体
python -m pytest tests/test_pet_engine.py -v
```

**结果**: 27 passed in 2.32s

---

## 三、修改文件清单

| 文件 | 改动 |
|---|---|
| `tests/test_pet_engine.py` | **新增 320 行** 27 个测试 |
| `src/pet/数值规则.py` | 重命名 → `numeric_rules_v1_legacy.py`(任务 2) |
| `src/server/routers/auth.py` | +2 secure 端点(任务 2) |
| `scripts/eval_ragas.py` | 适配 DeepSeek(任务 1) |

## 四、回滚方案

```bash
rm tests/test_pet_engine.py  # 单元测试可单独回滚
git checkout src/server/routers/auth.py  # secure 端点回滚
```

## 五、闭环

✅ P3-1 中文文件名 → 已重命名
✅ P3-2 鉴权切换 → 2 secure 端点
✅ P3-3 RAGAS → 3 指标 + 1 proxy
✅ P3-4 宠物测试 → **27/27 全过**

**4 项必还技术债全部 100% 清偿,自动化测试可重复执行。**
