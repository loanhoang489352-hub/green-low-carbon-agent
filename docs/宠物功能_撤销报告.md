# 宠物功能撤销报告

**时间**: 2026-06-14
**触发**: 用户反馈"融合效果未达目标"
**策略**: 显式禁用 + 后端保留(后续重做可复用)

---

## 一、撤销内容

| 类别 | 文件 | 处置 |
|---|---|---|
| **前端 JS** | `web/pet_desktop.js`(10KB) | ✅ **删除** |
| **前端 JS** | `web/pet_loader.js`(9KB) | ✅ **删除** |
| **前端 HTML** | `web/index.html` +155 行宠物 DOM/CSS/JS | ✅ **已还原** |
| **前端 缓存** | 3 个 no-cache meta | ✅ **已移除** |
| **Server 路由** | `src/server/routers/pet.py` 19 端点 | ⏸ 保留,默认不注册 |

## 二、保留(后续重做可复用)

| 类别 | 文件 | 价值 |
|---|---|---|
| **后端核心** | `src/pet/pet_engine.py`(474 行) | PetEngine 数值逻辑 |
| | `src/pet/species.py`(230 行) | 5 大生态系 + 抽卡 + 图鉴 |
| | `src/pet/seasonal.py`(220 行) | 季节活动 + 过期回收 |
| | `src/pet/ranking.py`(240 行) | PK 战力 + 段位 |
| | `src/pet/numeric_rules.py` | 数值规则 |
| | `src/pet/constants.py` + `__init__.py` + `skills.py` | DB + 注册 |
| | `src/pet/数值规则.py`(legacy) | 存档 |
| **路由** | `src/server/routers/pet.py` | 19 端点(env 禁用) |
| **测试** | `tests/test_pet_engine.py`(27 个) | 27/27 全过 |
| **数据** | `data/pet.db`(7 张表) | 用户养成数据不丢 |
| **备份** | `.backups/pet_rollback/` | 完整文件备份,3 文件 |

## 三、env 开关

**`src/server/routers/__init__.py`**:
```python
if os.environ.get("ENABLE_PET", "false").lower() in ("1", "true", "yes", "on"):
    from .pet import register_pet_routes
    register_pet_routes(registry)
```

| 配置 | 行为 |
|---|---|
| `ENABLE_PET=false`(默认) | 宠物路由**不注册**,前端无 UI,后端 PetEngine 代码仍可 import |
| `ENABLE_PET=true` | 恢复 19 端点 + 后续前端重做后可见 |

## 四、5 大后端核心模块 md5 指纹(确认 0 影响)

```
src/llm/client.py                  b9c8e5f2 (稳定)
src/rag/rag_engine.py              6e749cd1
src/user_profile/carbon_footprint.py 27a2f365
src/server/middleware/audit.py     e8b8d685
src/server/middleware/rate_limit.py 8c605fd3
```

## 五、验证

- ✅ `grep "pet\|🌱" web/index.html` → **0 个**引用
- ✅ `web/` 仅剩 `index.html` + `i18n.js`
- ✅ 默认启动后 `/api/pet/*` 路由 404(因未注册)
- ✅ `src/pet/` 模块完整保留
- ✅ `tests/test_pet_engine.py` 27/27 仍全过
- ✅ 5 大后端核心模块 0 改动

## 六、后续重做指南(供下一轮)

1. **重做前端 UI**:`cp -r .backups/pet_rollback/* web/`
2. **注册宠物路由**:`export ENABLE_PET=true` + 重启
3. **建议改进**:
   - 桌面精灵改用 sprite sheet(参考 eSheep 1000×500 PNG)
   - 加 Lottie 动画(需找开源"绿色精灵" Lottie JSON)
   - 加 sprite 帧动画(替代 CSS keyframe)

## 七、用户操作

1. **重启 agent**:`agent.bat`(或 Ctrl+C 重起)
2. **浏览器硬刷新**:`Ctrl + Shift + R`
3. **验证**:右下角不再有 🌱 浮动精灵,宠物面板不再出现

**如需重做**: 设置 `ENABLE_PET=true` + 还原 web 文件 + 重启。
