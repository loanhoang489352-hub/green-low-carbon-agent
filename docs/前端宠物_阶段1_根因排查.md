# 阶段 1: 前端宠物根因排查报告

**时间**: 2026-06-14
**目标**: 定位"加载中"+"无具象形象"两大故障

---

## 一、加载中 4 层根因(阶段 1.1-1.4)

### 1.1 资源路径(已检查)

| 检查项 | 状态 |
|---|---|
| CDN 外链 | ✅ 无外链 |
| 静态图片 404 | ✅ 无图片资源(全用 emoji) |
| 相对路径 | ✅ pet_desktop.js 路径正确 |

### 1.2 接口请求(已修复)

| 缺陷 | 修复 |
|---|---|
| `currentUserId` undefined 时不兜底 | ✅ 加 4 重兜底(window/global/localStorage/anonymous) |
| fetch 失败不显错 | ✅ 加 #pet-error 错误显示 |
| 无超时重试 | ⚠️ 阶段 4 加 |
| 跨域 | ✅ 同一域无跨域 |

### 1.3 渲染生命周期(已修复)

| 缺陷 | 修复 |
|---|---|
| `typeof currentUserId !== 'undefined'` 误判(全局 var 永远不是 undefined) | ✅ 改用 `typeof window.currentUserId !== 'undefined' && window.currentUserId` |
| 加载标识布尔变量重置 | ✅ 每次 refreshPetPanel 重置 errEl |
| 异步 await 链断 | ✅ try/catch 包全链路 |

### 1.4 渲染引擎(已修)

| 缺陷 | 修复 |
|---|---|
| 旧 `<div id="pet-floating-btn">` 静态 emoji | ✅ 重构为 `pet_desktop.js` 完整桌面精灵 |
| 无鼠标交互(点击/拖拽/hover 气泡) | ✅ 加拖拽 + 鼠标注视 + 点击抚摸 + 气泡 3 句/状态 |
| 无 6 状态动画 | ✅ 加 6 个 CSS keyframe(idle/super/hungry/sad/tired/critical) |

---

## 二、交互面板 DOM 结构(阶段 1.5)

### 2.1 保留功能 100%

```
✅ /api/pet/state      GET  查状态
✅ /api/pet/pat        POST 抚摸
✅ /api/pet/feed       POST 投喂
✅ /api/pet/apply      POST 录入行为
✅ /api/pet/inventory  GET  道具栏
✅ /api/pet/skills     GET  技能
✅ /api/pet/skill/today    今日复盘
✅ /api/pet/skill/lifetime 长期累计
✅ /api/pet/species    GET  5 大生态系
✅ /api/pet/gacha      POST 抽卡
✅ /api/pet/my         GET  我的宠物
✅ /api/pet/set-active POST 切换出战
✅ /api/pet/codex      GET  图鉴
✅ /api/pet/season     GET  季节活动
✅ /api/pet/season/status GET  季节进度
✅ /api/pet/season/claim POST 领奖
✅ /api/pet/ranking    GET  PK 排行榜
✅ /api/pet/pk         POST PK 对战
✅ /api/pet/friends    GET  好友列表
```

### 2.2 拆分 DOM 结构

```
#pet-desktop (固定位置精灵,80x80) ← 新增
  ├ #pet-desktop-bubble (气泡)
  ├ #pet-desktop-label (名字标签)
  └ #pet-desktop-body (精灵本体)

#pet-floating-btn (右下角浮动按钮) ← 保留,点击展开面板
#pet-panel (面板)
  ├ #pet-status-card (状态条 + 3 维值)
  ├ 互动按钮组(pat/feed/apply)
  ├ #pet-species-list (5 大生态系)
  ├ #pet-ranking (PK 排行榜)
  └ #pet-season (季节活动)
```

---

## 三、阶段 1 结论

✅ "加载中" 根因已定位 + 修复 70%
✅ DOM 结构盘点 + 功能保留确认 100%
⚠️ 还需阶段 4 加:预加载 + 进度条 + 重试 + 弱网兜底

**评估**: 可进入阶段 2/3 实施可视化重构。
