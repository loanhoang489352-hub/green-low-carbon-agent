# 绿色低碳智能体 — 生产依赖
# 锁版本,基于 2026-06 Python 3.14 环境实测

# LangGraph / LangChain
langgraph>=0.2.0
langchain-core>=0.3.0
typing-extensions>=4.12.0
langgraph-checkpoint-sqlite>=3.0.0

# RAG 与嵌入
chromadb>=1.5.0
sentence-transformers>=5.0.0
numpy>=2.0.0

# 数据存储 / 持久化
pydantic>=2.12.0
bcrypt>=5.0.0

# 知识/政策更新
requests>=2.33.0
beautifulsoup4>=4.12.0
trafilatura>=1.6.0
urllib3>=2.0.0

# Phase 3.3: 可选依赖(try/except 包裹,缺失时降级)
jieba>=0.42.0              # entity_linking.py 中文分词(可选,缺失时退化为基础实体匹配)
langchain-text-splitters>=0.3.0  # RAG Markdown 切分(缺失时退化为段落切分)
faiss-cpu>=1.7.0           # vector_store.py FAISS 后端(可选,默认走 ChromaDB)

# 配置
pyyaml>=6.0.0

# 调度(P4-A 引入)
APScheduler>=3.10.0,<4.0

# 异步 HTTP(P4-E 政策爬取用 httpx,requests 之外)
httpx>=0.27.0

# 测试
pytest>=8.0.0
pytest-asyncio>=0.23.0
